blurb=Roles grant permissions Jenkins-wide (global roles), on items matching a pattern (item roles), \
  or on agents matching a pattern (agent roles). Assign users and groups to roles on the Assign Roles page.
